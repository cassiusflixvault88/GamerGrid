# GamerGrid robots.txt
# Allow all friendly crawlers full access; expose the sitemap.

User-agent: *
Allow: /
Disallow: /admin
Disallow: /payment-success
Disallow: /api/

# Dynamic sitemap (refreshed <lastmod> on every crawl — Google's
# recommended replacement for the deprecated sitemap-ping endpoint).
Sitemap: https://gamer-grid.com/api/sitemap.xml
